packages:
  - name: assess
    functions:
      - name: draft
        runtime: 'nodejs:18'
        web: true
        environment:
          ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY}
        limits:
          timeout: 30000
